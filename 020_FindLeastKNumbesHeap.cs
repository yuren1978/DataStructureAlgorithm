using System;
using System.Collections.Generic;

class FindLeastKNumbers{
	
	static SortedSet<int> FindLeastKNumbers(int[] a, int k){
		SortedSet<int> leastKNumbers=new SortedSet<int>();
		int length=a.Length;
		for (int i=0; i<length; i++) {
			if(i<k)
		}
	}
	
	static void Main(){
		int[] a={2,4,1,3,1,5,6,7,3,10};
		k=4;
		SortedSet<int> leastKNumbers=FindLeastKNumbers(a, k); 
	}
}
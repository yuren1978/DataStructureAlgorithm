#include <iostream>
#include <string>

using namespace std;

vector<string> fullJustify(vector<string> &words, int L) {
	int wordCount=words.size();
	int lineStart=0;
	int lineEnd=0;
	while(getLine(lineStart, lineEnd)>=L && ){

		lineStart=lineEnd;
	}
}